DOCKER

ls : list
ps : process status
df : disk free
rm : remove

docker :
  ps : список
    docker ps     : контейнеры : запущенные
    docker ps -a  : контейнеры : запущенные, остановленные и завершившиеся
    docker ps -q  : ID контейнеров : запущенных
  logs : логи
    docker logs CTN        : логи
    docker logs -t CTN     : логи + временные метки
    docker logs -n NUM CTN : логи : tail
    docker logs -f CTN     : логи : live
  stats : ресурсы
    docker stats    : статистика : запущенные
    docker stats -a : статистика : запущенные, остановленные и завершившиеся
    docker ps -qa : ID контейнеров : запущенных, остановленных и завершившихся
  top : процессы
    docker top CTN : процессы
  port : порты
    docker port CTN : проброшенные порты
  inspect : анализ
    docker inspect ANY : подробная информация
  exec : команды
    docker exec CTN CMD     : команда
    docker exec -d CTN CMD  : команда : фоновый режим
    docker exec -i CTN CMD  : интерактивная команда
    docker exec -it CTN CMD : интерактивная команда : TTY
  stop + kill : завершение
    docker stop CTN : SIGTERM > timeout > SIGKILL
    docker kill CTN : SIGKILL
  rm : удаление
    docker rm CTN    : удаление
    docker rm -f CTN : удаление + остановка
    docker rm -v CTN : удаление + удаление связанных анонимных томов
  run : запуск
    docker run IMG                    : запуск
    docker run -d IMG                 : запуск : фоновый режим
    docker run -i IMG CMD             : запуск + интерактивная команда
    docker run -it IMG CMD            : запуск + интерактивная команда : TTY
    docker run --name NAM IMG         : запуск + именование
    docker run -p HST_PRT:CTN_PRT IMG : запуск + проброс порта
    docker run -e KEY=VAL IMG         : запуск + экспорт переменной окружения
    docker run -v HST_PTH:CTN_PTH IMG : запуск + монтирование тома/директории
  cp : копирование
    docker cp CTN:PTH HST_PTH : контейнер > хост
    docker cp HST_PTH CTN:PTH : хост > контейнер
  images : список
    docker images    : образы
    docker images -a : образы + промежуточные + dangling
  build : сборка
    docker build -t NAM:TAG PTH : сборка образа : dockerfile
  tag : тегирование
    docker tag IMG NAM:TAG : тегирование
  pull : загрузка из реестра
    docker pull IMG:TAG : загрузка образа
  push : загрузка в реестр
    docker push USR/IMG:TAG : отправка образа
  rmi : удаление
    docker rmi IMG    : удаление : не используемые
    docker rmi -f IMG : удаление : используемые тоже

docker container :
  ls : список
    docker container ls     : контейнеры : запущенные
    docker container ls -a  : контейнеры : запущенные, остановленные и завершившиеся
    docker container ls -q  : ID контейнеров : запущенных
    docker container ls -qa : ID контейнеров : запущенных, остановленных и завершившихся
  logs : логи
    docker container logs CTN        : логи
    docker container logs -t CTN     : логи + временные метки
    docker container logs -f CTN     : логи : live
    docker container logs -n NUM CTN : логи : tail
  stats : ресурсы
    docker container stats    : статистика : запущенные
    docker container stats -a : статистика : запущенные, остановленные и завершившиеся
  top : процессы
    docker container top CTN : процессы
  port : порты
    docker container port CTN : проброшенные порты
  exec : команды
    docker container exec CTN CMD     : команда
    docker container exec -d CTN CMD  : команда : фоновый режим
    docker container exec -i CTN CMD  : интерактивная команда
    docker container exec -it CTN CMD : интерактивная команда : TTY
  stop + kill : завершение
    docker container stop CTN : SIGTERM > timeout > SIGKILL
    docker container kill CTN : SIGKILL
  rm : удаление
    docker container rm CTN    : удаление
    docker container rm -f CTN : удаление + остановка
    docker container rm -v CTN : удаление + удаление связанных анонимных томов
  run : запуск
    docker container run IMG                    : запуск
    docker container run -d IMG                 : запуск : фоновый режим
    docker container run -i IMG CMD             : запуск + интерактивная команда
    docker container run -it IMG CMD            : запуск + интерактивная команда : TTY
    docker container run --name NAM IMG         : запуск + именование
    docker container run -p HST_PRT:CTN_PRT IMG : запуск + проброс порта
    docker container run -e KEY=VAL IMG         : запуск + экспорт переменной окружения
    docker container run -v HST_PTH:CTN_PTH IMG : запуск + монтирование тома/директории
  cp : копирование
    docker container cp CTN:PTH HST_PTH : контейнер > хост
    docker container cp HST_PTH CTN:PTH : хост > контейнер

docker image : образы
  ls : список
    docker image ls    : образы
    docker image ls -a : образы + промежуточные + dangling
  build : сборка
    docker image build -t NAM:TAG PTH : сборка образа : dockerfile
  tag : тегирование
    docker image tag IMG NAM:TAG : тегирование
  pull : загрузка из реестра
    docker image pull IMG:TAG : загрузка образа
  push : загрузка в реестр
    docker image push USR/IMG:TAG : отправка образа
  rm : удаление
    docker image rm IMG    : удаление : не используемые
    docker image rm -f IMG : удаление : используемые тоже

docker network : сети
  ls : список
    docker network ls    : сети
    docker network ls -q : ID сетей
  create : создание
    docker network create NET : создание
  rm : удаление
    docker network rm NET : удаление
  connect : подключение
    docker network connect NET CTN : подключение
  disconnect : отключение
    docker network disconnect NET CTN : отключение

docker system : администрирование
  df : свободное место
    docker system df    : место
    docker system df -v : место : подробно
  prune : очистка
    docker system prune    : остановленные контейнеры, пустые сети, образы без тега, кеш
    docker system prune -a : остановленные контейнеры, пустые сети, образы, кеш
